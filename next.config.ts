import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export', // Habilita la exportación estática
  // Agrega otras configuraciones aquí si es necesario
};

export default nextConfig;
